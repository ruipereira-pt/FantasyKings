# Production Environment Variables Template
# ⚠️ DO NOT COMMIT THIS FILE WITH REAL VALUES!
# This is a template for reference only.
# Copy values to Bolt.host Dashboard → Environment Variables

# Supabase Configuration
# These MUST be set in Bolt.host dashboard for production builds
# Variable names MUST start with VITE_ prefix for Vite to expose them

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# Instructions:
# 1. Copy the values from your local .env file
# 2. Go to Bolt.host Dashboard → Your Project → Environment Variables
# 3. Add each variable with EXACT name (case-sensitive):
#    - VITE_SUPABASE_URL
#    - VITE_SUPABASE_ANON_KEY
# 4. Mark them for "Build" or "Both" (not just "Runtime")
# 5. Trigger a new deployment

